# Database connection URL for PostgreSQL
# This should match the database configuration in docker-compose.yaml
# For production, use a hosted PostgreSQL like Neon: postgresql://user:pass@host/db?sslmode=require
DATABASE_URL=postgresql://postgres:password@localhost:54321/electric

# Authentication secret - REQUIRED
# Generate a strong secret (minimum 32 characters): openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-here-min-32-chars

# Production URL(s) for trusted origins (comma-separated)
# Required for auth to work in production
# Example: https://buzzin.pages.dev,https://buzzin.example.com
APP_URL=

# Google OAuth (optional for local dev, required for production)
# Get these from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Admin email whitelist (comma-separated)
# Only these emails can access the admin dashboard
ADMIN_EMAILS=admin@example.com

# Anthropic API Key for AI-generated wrong answers
# Get from https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Electric Cloud Configuration
# For production, get these from https://dashboard.electric-sql.cloud/
# For local dev, leave unset to use local Electric at http://localhost:30000
# Start local Electric with: pnpm backend:up
ELECTRIC_URL=
ELECTRIC_SOURCE_ID=
ELECTRIC_SECRET=
